I"<p><br />
터미널에서 아래의 명령어로 캐시 삭제를 수행할 수 있다.</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
git rm -r --cached .
git add .
git commit -m "cache clear"

</code></pre></div></div>
:ET